<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_order_form_invoice_blocking_reason" model="ir.ui.view">
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <field name="client_order_ref" position="after">
                <field name="invoice_blocking_reason_id" />
            </field>
            <!--xpath
                expr="//button[@name='%(sale.action_view_sale_advance_payment_inv)d'][contains(concat(' ',normalize-space(@class),' '),' btn-primary ')]"
                position="attributes"
            >
                <attribute
                    name="attrs"
                >{'invisible': ['|', ('invoice_status', '!=', 'to invoice'), ('invoice_blocking_reason_id', '!=', False)]}</attribute>
            </xpath>
            <xpath
                expr="//button[@name='%(sale.action_view_sale_advance_payment_inv)d'][not(contains(concat(' ',normalize-space(@class),' '),' btn-primary '))]"
                position="attributes"
            >
                <attribute
                    name="attrs"
                >{'invisible': ['|', '|', ('invoice_status', '!=', 'no'), ('state', '!=', 'sale'), ('invoice_blocking_reason_id', '!=', False)]}</attribute>
            </xpath-->
        </field>
    </record>

</odoo>
